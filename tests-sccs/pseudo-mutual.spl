// The SCC analysis should strip away the `mutual` block, since the functions
// aren't actually defined in terms of each other.

mutual {

  f(x) :: a -> b {
    return g(x);
  }

  g(n) :: Int -> Int {
    return n + 1;
  }

}

main() {
  print(f(41));
}
